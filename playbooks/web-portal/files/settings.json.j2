{
   "machines": [
        {% for machine in webportal.vars.machines  %}
        {
            "id": "{{machine.id}}",
            "label": "{{machine.label}}",
            "description": "{{machine.description}}",
            "hostname": "{{machine.hostname}}",
        }
        {% endfor %}
    ]

}